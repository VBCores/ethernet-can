#!/bin/sh
set -eu
case "${1:-}" in
    remove|purge)
        if [ -d /run/systemd/system ]; then
            systemctl daemon-reload
        fi
        # User JSON files are deliberately not owned or deleted by this package.
        ;;
esac
